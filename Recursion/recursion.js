// Recursion means that, koi bhi function hai woh apnay apko call kar rha hai, khood se. When function calls itself its called recursion.
// But agar koi function apnay apko call karta rhai ga, toh its means woh infinite tak chalta rhai ga.
// But if a function call itself, then there must be an end point.

// let count = 1;
// function print(num) {
//     if (count > num) {
//         return;
//     }
//     console.log(`Count = ${count}`);
//     count++;
//     print(num);
// }


// print(10);